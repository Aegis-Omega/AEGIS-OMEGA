{
  "name": "aegis-tactical-dashboard",
  "workspaces": ["backend"],
  "scripts": {
    "dev-backend": "npm run dev --prefix backend",
    "dev": "npm run dev-backend"
  },
  "devDependencies": {
    "concurrently": "latest"
  }
}
