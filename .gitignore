# Operator credential drop zone — never commit
.operator-secrets

# Compiled and build artifacts
*.o
*.obj
*.exe
*.dll
*.so
*.a
*.out
dist/
build/

# Dependencies
node_modules/
**/node_modules/
target/
**/target/

# Logs and temp files
*.log
*.tmp
*.swp

# Editors
.vscode/
.idea/
.ipynb_checkpoints/
*.jupyterlab-workspace
Untitled*
.DS_Store
Thumbs.db

# Environment
.env
.env.local
*.env.*
!*.env.example
# AEGIS operator profiles / secret-bearing exports — never commit
*Production Profile*
*production-profile*
*.profile.txt
.vercel/

# Auto-gate crash recovery checkpoint (never commit)
scripts/.auto-gate-checkpoint.json

# Coverage
coverage/
htmlcov/
.coverage

# Python specific
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
.eggs/
.mypy_cache/
.pytest_cache/
.tox/
nosetests.xml

# Claude/Codex local state
.claude/settings.local.json
.vercel

# Metacognitive live chain — all JSONL files are runtime state, not source
.claude/metacog/chain.jsonl
.claude/metacog/verdicts.jsonl
.claude/metacog/seals.jsonl
.claude/metacog/ratifications.jsonl

# Content-addressed delta hook runtime state — session-local, not committed
.claude/session-state.json

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example
worker-configuration.d.ts

# Sovereign-mesh pipeline run artifacts (architect/artisan/auditor) — regenerated per sprint, not source
state/
