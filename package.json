{
  "name": "aegis-tactical-dashboard",
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "dev-backend": "npm run dev --prefix backend",
    "dev": "npm run dev-backend",
    "postinstall": "node scripts/cloudflare-enterprise-replay.mjs"
  },
  "devDependencies": {
    "concurrently": "latest"
  }
}
