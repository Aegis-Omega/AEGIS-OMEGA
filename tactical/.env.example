# Bridge URL — defaults to localhost:7890 in dev
VITE_BRIDGE_URL=https://aegis-vertex.aegisomega.com

# API key — if set here it overrides the UI key-entry prompt
# In dev: any aegis_* key works when bridge has no Supabase configured
# VITE_AEGIS_API_KEY=aegis_your_key_here
