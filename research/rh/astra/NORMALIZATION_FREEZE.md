# AEGIS Ω RH normalization freeze — V1

Status: `PARTIALLY_FROZEN`  
RH: `NOT_PROVEN`  
Authority effect: `NONE`

This file is deliberately not a fake “all conventions frozen” certificate. The current Lean proofline has machine-bound local objects, but the final explicit-formula normalization has not yet been reduced to one canonical cross-runtime specification.

Before `EXPLICIT_FORMULA_THEOREM` may leave `OPEN`, Astra/Codex must extract and bind, from exact source bytes, the Fourier transform convention, Mellin transform convention, completed-zeta/gamma normalization, zero multiplicity and truncation convention, trivial-zero treatment, prime-power weights, Archimedean term, admissible test-function class, conjugation/autocorrelation convention, and every boundary/distributional term.

The current machine-bound height-truncation contract is evidence for the zero-side summation infrastructure. It is **not** automatically the summation convention required by the final explicit formula.

A normalization field can move from `UNFROZEN` only together with an exact source coordinate and a theorem/definition binding. Silent convention changes are forbidden.
