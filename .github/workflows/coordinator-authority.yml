name: AEGIS Coordinator Authority

on:
  pull_request:
    paths:
      - "agents/**"
      - "harness/**"
      - "vertex/Dockerfile"
      - "vertex/startup_guard.py"
      - "vertex/secure_serve.py"
      - "vertex/audit_chain_v2.py"
      - "scripts/auto-gate.py"
      - "scripts/validate-daybreak-blue-boundary.py"
      - ".github/workflows/agent-dispatch.yml"
      - "sovereign-omega-v2/python/tests/test_daybreak_blue_audit_chain.py"
      - "sovereign-omega-v2/python/tests/test_coordinator_authority.py"
      - ".github/workflows/coordinator-authority.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: coordinator-authority-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  coordinator-authority:
    name: aegis / coordinator-authority
    runs-on: ubuntu-latest
    timeout-minutes: 10
    services:
      redis:
        image: redis:7.4-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
    steps:
      - name: Checkout candidate
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install exact test dependencies
        run: python -m pip install --disable-pip-version-check pytest==8.4.1 httpx==0.28.1 redis==6.2.0 PyYAML==6.0.2 anthropic==0.52.0 fastapi==0.115.0

      - name: Compile security-bound Python entrypoints
        run: python -m py_compile scripts/auto-gate.py scripts/validate-daybreak-blue-boundary.py vertex/startup_guard.py vertex/audit_chain_v2.py vertex/secure_serve.py

      - name: Run V2 registry authority tests
        run: pytest -q sovereign-omega-v2/python/tests/test_skill_authority.py

      - name: Run coordinator fail-closed tests
        run: pytest -q sovereign-omega-v2/python/tests/test_coordinator_authority.py

      - name: Run Daybreak audit-chain tests
        env:
          AEGIS_TEST_REDIS_URL: redis://127.0.0.1:6379/15
        run: pytest -q sovereign-omega-v2/python/tests/test_daybreak_blue_audit_chain.py

      - name: Validate Daybreak Blue external boundary
        run: python scripts/validate-daybreak-blue-boundary.py
